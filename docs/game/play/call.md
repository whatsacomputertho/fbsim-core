# Call module

The `call` module contains the `PlayCall` enum and `PlayCallSimulator` struct. Given a `PlayContext` and  `FootballTeamCoach` properties, the `PlayCallSimulator` chooses a `PlayCall` enum instance.
